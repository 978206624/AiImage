# Product Spec — CANVAS AI 图像画廊

## 产品概述

CANVAS 是一个 AI 图像生成与展示平台，用户注册账号后通过充值码获得生图额度，在网站上输入提示词生成 AI 图像，作品永久保存在账号下。

**目标用户**：对 AI 生图感兴趣但不想折腾 API 对接的普通用户。设计师、自媒体创作者、AI 绘画爱好者，通过淘宝购买充值码兑换积分使用，可在账号下查看历史创作。

**核心价值**：零门槛使用 GPT Image2 生图能力。注册 → 体验赠送积分 / 充值码兑换 → 写提示词 → 出图 → 历史永久保存。同时提供精选画廊（精选作品 + 可直接套用的提示词模板，按分类组织）降低创作门槛。

**商业模式**：淘宝出售充值码（一次性兑换码），每次生图消耗 0.07 积分（约 7 分钱），后端通过第三方中转站调用 GPT Image 2 接口赚取差价。新用户注册赠送 0.21 积分（3 张免费体验）作为拉新激励。

## 应用场景

- **小红书博主**：注册账号后用赠送积分免费体验 3 张，满意后淘宝买充值码继续创作。从画廊按分类挑一张喜欢的模板，改关键词，一键生成 4 张，下载发帖。所有作品在历史记录里随时回看。

- **淘宝店主做主图**：上传产品照片作为参考图，写提示词生成多张选最好的当主图。从历史记录里直接复用上次的成功参数，微调一下重新出图。

- **AI 绘画爱好者**：逛画廊找灵感，看到喜欢的图复制提示词，跳转生图页面微调参数。所有创作历史保留在账号下，可以任意时间回看下载。

- **设计师找参考**：在生图页用「选择风格」弹窗叠加多个风格预设 + 提示词快速出一批参考图。从历史记录里复用之前调好的参数组合，省去重复填写时间。

- **学生做 PPT 配图**：从画廊按分类找现成模板，替换关键词生成，下载插入 PPT。账号下有完整历史，下次做类似 PPT 直接翻历史。

## 功能需求

### 核心功能

**用户账号系统**
- 邮箱 + 密码注册：填写邮箱和密码 → 系统通过 SMTP 发送激活链接到邮箱 → 用户点击链接激活账号 → 自动登录并赠送 0.21 积分（3 张免费体验）
- 邮箱 + 密码登录：登录成功后下发 HTTP-only Session Cookie 维持登录状态
- 忘记密码：用户填邮箱 → 系统发送重置链接 → 用户点击后设置新密码
- 账号中心 `/account`：展示当前余额、累计消费、充值码兑换入口、账单记录、修改密码
- 账号下拉菜单：导航栏右上角头像点击展开，包含账号中心、历史记录、退出登录
- 防刷规则：同邮箱仅赠送一次注册积分；同 IP 24 小时内最多注册 3 次

**充值码系统**
- 充值码格式：`CV-XXXXXXXXXXXXXXXX`（16 位大写字母+数字，CV- 前缀）
- 一次性使用：兑换成功后充值码作废，对应积分额度转入用户账号余额
- 老 Key 兼容：v1.0 时代淘宝销售的 Key 与新生成的充值码格式一致，老用户注册账号后在账号中心输入老 Key 即可兑换为余额（剩余积分一次性转入），原 Key 标记作废
- 兑换入口：账号中心页面 `/account` 的「充值码兑换」区域

**AI 生图**
- 用户登录后输入提示词 → 选择画面比例、质量、数量 → 点击「开始生图」 → 系统验证账号余额 → 创建本地生图任务并返回 taskId → 后台 worker 按并发限制调用中转站生图接口 → 拉取或解析中转站返回的图片 → 上传到自有 OSS（路径 `gallery/users/<uid>/<timestamp>_<rand>.png`）→ 写入历史记录（user_id、prompt、参数 JSON、OSS 图片 URL）→ 前端轮询任务状态并在完成后展示
- 未登录用户可访问生图页、可编辑参数、可上传参考图，但点击「开始生图」时弹出登录窗
- 支持上传参考图（最多 12 张，支持拖拽排序）→ 参考图 URL 传给接口的 image 字段
- 支持风格预设 → 提示词输入框内的「选择风格」按钮 → 弹窗以宫格形式展示全部预设（封面图 + 名称） → 可多选 → 关闭弹窗后选中预设对应的 prompt 前缀依次拼接到用户提示词前面
- 生成数量 1/2/4 张，每张独立调用接口，每张扣 0.07 积分（成功才扣，失败不扣）
- 生成失败的不计入历史记录

**历史记录**
- 数据归属：每条历史记录关联用户账号（user_id），永久保留
- 内容：缩略图、prompt 摘要、模型标签、画面比例、生成时间、参数信息（质量/数量/风格预设）、原图 OSS URL
- 操作：
  - 查看：点击图片 Lightbox 全屏预览
  - 下载：单张下载 / 批量下载全部
  - 复用参数：一键将当时的 prompt + 参考图 + 比例 + 质量 + 数量 + 风格预设全部填回生图面板
- 展示位置：
  - 生图页左栏底部「最近创作」：最近 10 条简版列表（缩略图 + 时间），点击复用参数；未登录时显示登录引导
  - 独立页面 `/history`：完整列表，4 列网格布局，按时间倒序排列，支持模型标签和画面比例筛选，分页加载（每页 20 条）

**画廊浏览**
- 瀑布流展示管理员在后台上传的精选图片与提示词模板（统一为画廊条目，可选 `title` 标识模板名）
- 支持按模型、风格、分类三组 chips 并排筛选
- hover 显示标题 + 提示词 + 操作按钮（复制提示词、用此生图）
- 点击「用此生图」→ 将提示词填入生图页面并跳转（未登录可以填，开始生图时拦截）
- 卡片以真实图片宽高比占位（数据库存 `width` / `height`），加载过程不出现重排抖动

**首页展示**
- Hero 区域：品牌展示 + CTA 引导（未登录显示「立即注册」，已登录显示「开始创作」）
- 支持的生图模型：当前仅 GPT Image2 可用；Google Nano Banana Pro、Midjourney V7 作为即将支持模型展示（不可选），为后续扩展留位
- 今日精选：展示后台标记为「精选」的画廊图片
- 底部 CTA：引导进入画廊或生图页面

### 辅助功能

**图片预览**
- 点击生成的图片 / 历史记录图片 → Lightbox 全屏预览
- 显示使用的模型和提示词信息

**图片下载**
- 单张下载 / 批量下载全部生成结果
- 历史记录页面支持单条下载 / 批量下载选中

**Toast 通知**
- 操作反馈（注册成功、激活成功、复制成功、生成完成、余额不足、兑换成功等）

### 管理后台功能

**用户管理**（新增）
- 查看用户列表（邮箱、当前余额、累计消费、注册时间、最后登录时间、状态）
- 手动充值/扣减积分（操作有日志记录）
- 封禁/解封账号
- 重置用户密码（生成临时密码，用户登录后强制修改）
- 查看单个用户的生图历史和充值历史

**充值码管理**（原「Key 管理」改名）
- 批量生成充值码（指定积分额度）
- 查看所有充值码列表（充值码、积分额度、状态[未使用/已使用]、使用者邮箱、创建时间、使用时间）
- 禁用/启用充值码
- 老 Key 兼容显示：可看到 v1.0 时代生成的 Key，标注「v1.0 老 Key」状态

**画廊管理**（合并原模板管理）
- 三个 Tab：图片 / 分类 / 风格预设
- 图片 Tab：上传图片到阿里云 OSS、填写信息（标题 title 可选、prompt、模型标签、风格标签、分类）、标记「精选」、上下架管理；入库时探测并保存图片的 width / height 用于前台占位
- 分类 Tab：分类 CRUD（名称、排序），供画廊筛选与编辑使用
- 风格预设 Tab：风格预设 CRUD（预设名、长描述、封面图、prompt 前缀），仅供生图页「选择风格」弹窗使用，不再有独立前台页面

**模型管理 / VectorEngine 接入配置**
- 管理 GPT Image2、Google Nano Banana 系列、Midjourney V7 等可用生图模型
- 字段至少包含：前端展示名、provider、真实 model id / 接口类型、是否启用、是否前台可选、排序、计费方式（按量 / 按次）、平台成本、用户扣费积分、并发上限、超时时间
- 模型配置作为前台模型选择、后台画廊模型标签、历史记录筛选、扣费规则的统一来源，避免在多个组件中硬编码模型列表
- GPT Image2 前端展示名固定为 `GPT Image2`，真实上游模型 ID 为 `gpt-image-2`

**生图任务管理**
- 查看任务列表：任务 ID、用户、provider、模型、状态、进度、消耗积分、创建时间、更新时间
- 支持按状态筛选：pending / processing / completed / failed
- 失败任务显示失败原因、上游响应摘要、重试次数
- 支持管理员对失败任务执行重试、标记失败、查看原始参数

**Worker 状态监控**
- 展示 worker 最近心跳时间、当前处理中任务数、最近失败任务数
- 标记长时间 stuck 的 `processing` 任务，供管理员手动恢复为 pending 或标记失败
- 展示各 provider/model 当前并发上限与实际处理情况

**系统设置**
- 配置 VectorEngine / 中转站 API 地址（base_url）
- 配置 VectorEngine / 中转站 API Key
- 配置当前启用的 provider 与模型开关
- 配置阿里云 OSS（AccessKey、Bucket、Region、Endpoint）
- 配置默认单次生图积分消耗（默认 0.07），多模型接入后以模型管理中的用户扣费积分为准
- 配置注册赠送积分（默认 0.21）
- 配置 SMTP 邮件服务（Host、Port、用户名、密码、发件人地址）
- 配置管理员账号（默认 admin）和管理员密码

## UI 布局

### 整体结构

单页应用，顶部固定导航栏（60px 高），路由切换：

公开页面（无需登录）：首页、画廊、登录、注册、邮箱激活、忘密重置
账号页面（需登录）：账号中心 `/account`、历史记录 `/history`
混合页面：生图页（可访问可编辑，点「开始生图」校验登录）

默认暗色主题，编辑风格（editorial-monocle dark），使用 oklch 色彩空间；导航栏提供亮色 / 暗色主题切换，并记忆用户选择。

### 导航栏

- 左侧：品牌 Logo「CAN◈VAS」
- 中间：页面导航链接（首页、画廊、生图）
- 右侧：
  - **未登录**：「登录」「注册」按钮 + CTA 按钮「开始创作」
  - **已登录**：余额徽章（显示当前积分） + 头像下拉菜单（账号中心、历史记录、退出登录）+ CTA 按钮「开始创作」
  - 主题切换按钮：在亮色 / 暗色主题间切换，选择结果保存在浏览器本地
- 背景半透明 + 毛玻璃效果，固定在顶部

### 首页

- **Hero 区域**：全屏高度，左下角内容（eyebrow 标签 + 大标题 + 副标题 + 双按钮），背景渐变 + 噪点纹理。CTA 按钮根据登录状态变化：未登录显示「立即注册」，已登录显示「开始创作」
- **模型展示区**：2 列网格，模型卡片（渐变色封面 + 模型名 + 描述 + 作品数量）。当前仅 GPT Image2 可用；Google Nano Banana Pro 与 Midjourney V7 作为即将支持模型展示（available=false），具体 model id 以中转站文档为准
- **今日精选区**：不规则网格（1 大 4 小），hover 显示模型标签和 prompt
- **底部 CTA**：居中标题 + 描述 + 双按钮

### 画廊页

- **筛选栏**：sticky 在导航栏下方，三组 chips 自上而下排布：模型 chips（全部 / GPT Image2 / Nano Banana Pro / Midjourney V7）+ 风格 chips（全部 / 写实 / 动漫 / 油画 等）+ 分类 chips（全部 / 人像 / 风景 / 建筑 / 动漫 / 抽象 / 产品 / 概念艺术 等）
- **瀑布流网格**：4 列 masonry 布局，卡片以图片真实宽高比占位（数据库提供 width/height，加载过程不发生 reflow），hover 显示半透明遮罩（顶部 title 可选 + 模型标签 + prompt 摘要 + 操作按钮「复制提示词」「用此生图」）

### 生图页

三栏布局：左侧 256px / 中间自适应 / 右侧 272px

**左栏 - 模型选择 + 历史记录**：
- 模型卡片列表（当前只有 GPT Image2 可选，高亮选中态）
- 分隔线
- 「最近创作」标题 + 「查看全部」链接（跳 `/history`）
- 历史记录简版列表：最近 10 条，每条缩略图（48×48）+ 时间，点击复用当时的全部参数到生图面板
- 未登录时显示「登录后查看创作历史」+ 内联登录按钮
- 已登录但无历史时显示「暂无创作记录，开始你的第一次创作」

**中栏 - 创作区**：
- 标题行：「生图工作台」+ 当前模型 badge
- 生图提示词：多行文本框，框内右上角悬浮「选择风格」按钮（带已选数量徽标，0 时只显示文字），点击打开风格选择弹窗
- 风格选择弹窗：居中模态，宫格展示全部风格预设卡片（封面图 + 名称 + 简短描述截断），可多选（点击卡片切换勾选态），底部显示「已选 N 个 / 应用 / 清空」操作按钮，应用后关闭弹窗、按钮徽标更新数字、选中预设的 prompt 前缀按选中顺序拼接到提示词框正文前
- 参考图区域：拖拽上传区，6 列网格展示已上传图片（支持拖拽排序、删除、编号显示），最多 12 张
- 「开始生图」主按钮（未登录点击 → 弹出登录窗；余额不足 → 弹出兑换码窗或引导跳淘宝）
- 生成结果区：根据数量 1/2/4 列网格展示，点击可 Lightbox 预览

**右栏 - 参数面板**：
- 画面比例：3x2 网格按钮（2:3 / 1:1 / 16:9 / 9:16 / 4:3 / 3:4）
- 生图质量：滑块（low / medium / high）
- 生成数量：3 个按钮（1 / 2 / 4）

### 登录 / 注册 / 邮箱激活 / 忘密重置

居中卡片布局，左侧品牌图，右侧表单。

- **注册页 `/register`**：邮箱 + 密码 + 确认密码 + 「我已同意服务条款」 → 「注册」按钮 → 跳「请前往邮箱激活」提示页
- **登录页 `/login`**：邮箱 + 密码 + 「忘记密码？」链接 + 「登录」按钮 + 「还没账号？立即注册」
- **邮箱激活 `/verify-email?token=xxx`**：进入页面自动校验 token → 成功跳转生图页 + Toast「激活成功，已赠送 0.21 积分」 / 失败显示「链接已过期」+ 重发按钮
- **忘记密码 `/forgot-password`**：邮箱输入框 → 「发送重置链接」按钮 → 「邮件已发送，请查收」提示
- **重置密码 `/reset-password?token=xxx`**：新密码 + 确认密码 → 「确认重置」 → 自动登录并跳转生图页

### 账号中心 `/account`

- **顶部信息条**：用户邮箱 + 当前余额（醒目大字） + 累计消费（次要）
- **充值码兑换区**：标题「充值码兑换」+ 输入框 + 「兑换」按钮，兑换成功 Toast 显示「+X 积分到账」
- **账单记录**：Tab 切换（充值记录 / 消费记录），列表展示
  - 充值记录：时间 + 充值码（脱敏） + 积分额度
  - 消费记录：时间 + prompt 摘要 + 消耗积分 + 缩略图
- **安全设置**：修改密码（旧密码 + 新密码 + 确认）

### 历史记录 `/history`

- **顶部筛选栏**：sticky，模型 chips + 画面比例 chips + 时间范围（今日 / 本周 / 本月 / 全部）
- **网格布局**：4 列，每个卡片包含缩略图、prompt 摘要、模型标签、生成时间、操作按钮（查看 / 下载 / 复用参数）
- **批量操作栏**：选中多张时浮现，「批量下载」按钮
- **分页加载**：每页 20 条，下拉无限滚动
- **空状态**：「还没有创作记录，去生图吧」+ 跳转生图页 CTA

### 管理后台（`/admin`）

- 登录页：账号输入框 + 密码输入框 + 登录按钮；错误统一提示「账号或密码错误」（不区分账号或密码错，防止账号枚举）；连续失败达上限时提示「登录失败次数过多，请 15 分钟后再试」
- 左侧导航：用户管理、充值码管理、画廊管理（含图片 / 分类 / 风格预设三个 Tab）、系统设置
- 右侧内容区：对应的 CRUD 表格/表单界面
- 简洁实用风格，不需要花哨设计

## 用户使用流程

### 新用户首次使用

1. 打开 CANVAS 网站
2. 点击「注册」→ 填邮箱 + 密码 → 系统发送激活邮件
3. 进入邮箱点击激活链接 → 跳转生图页 + Toast「激活成功，已赠送 0.21 积分（3 张免费体验）」
4. 输入提示词（或从模板库选一个套用）
5. 调整参数（比例、质量、数量）
6. 点击「开始生图」
7. 等待生成完成，图片自动保存到账号历史记录
8. 下载满意的图片
9. 想继续创作 → 余额耗尽时引导淘宝购买充值码

### 老 Key 用户迁移

1. 打开 CANVAS 网站，发现需要注册（v1.0 的 Key 输入入口已不存在）
2. 注册账号 → 激活 → 自动跳转生图页
3. 进入「账号中心 `/account`」
4. 在「充值码兑换」处输入老 Key（CV-XXX）→ 剩余积分转入账号余额，原 Key 作废
5. 正常使用

### 通过画廊找灵感 / 套用模板

1. 浏览画廊（无需登录），按模型 / 风格 / 分类组合筛选
2. hover 查看 title（模板名）和 prompt
3. 点击「用此生图」
4. 跳转到生图页面，prompt 已填入（可继续编辑参数）
5. 如需叠加风格 → 点提示词框内的「选择风格」按钮 → 弹窗内宫格选择
6. 点击「开始生图」→ 未登录则弹出登录窗 → 登录后继续生图

### 复用历史记录创作

1. 已登录用户进入生图页面
2. 左栏底部「最近创作」看到最近 10 条历史
3. 点击其中一条 → prompt + 参考图 + 比例 + 质量 + 数量 + 风格预设全部回填
4. 修改 prompt 或参数微调
5. 点击「开始生图」

### 充值

1. 余额不足时，生图前弹出提示「积分不足，请购买充值码」
2. 跳转到淘宝购买（外链）
3. 拿到充值码后回到账号中心 `/account`
4. 输入充值码 → 兑换成功 → 余额到账
5. 回到生图页继续创作

### 找回密码

1. 登录页点「忘记密码？」
2. 输入邮箱 → 系统发送重置链接
3. 邮箱收到链接，点击进入重置密码页
4. 设置新密码 → 自动登录

### 管理员操作

1. 访问 `/admin`，输入账号（默认 admin）和密码登录
2. 用户管理：监控注册、手动调整余额、处理投诉
3. 充值码管理：批量生成 → 复制充值码列表 → 淘宝发货
4. 画廊管理：图片 Tab 上传精选图片或提示词模板 → 填写 title / prompt / 模型 / 风格 / 分类 → 标记精选 → 上架；分类 Tab 维护分类；风格预设 Tab 维护用于生图页弹窗的预设
5. 系统设置：配置中转站、OSS、SMTP、注册赠送、管理员密码

## AI 能力需求

| 能力类型 | 用途说明 | 应用位置 |
|---------|---------|---------|
| 图像生成 | 根据用户提示词 + 参考图 + 参数生成 AI 图像 | 用户点击「开始生图」时，通过第三方中转站调用 GPT Image 2 |

## 技术方向

| 维度 | 选择 | 理由 |
|------|------|------|
| 产品类型 | Web | 面向普通用户，无需安装，浏览器直接访问 |
| 推荐技术栈 | Next.js + TypeScript + Tailwind CSS | 全栈框架，API Routes 做后端中转，前后端同项目 |
| 数据存储 | MySQL（阿里云 RDS） | 用户、充值码、画廊、模板、生成记录、邮件令牌 |
| 文件存储 | 阿里云 OSS | 画廊封面、模板封面、参考图临时文件、用户生成图（永久保存） |
| 邮件服务 | SMTP（推荐阿里云邮件推送或腾讯云 SES） | 注册激活、密码重置 |
| 鉴权 | HTTP-only Cookie + JWT（jose 签名） | 安全、跨标签共享、不被 JS 窃取 |
| 部署方式 | Docker / 阿里云 ECS | 需要后端服务，不能纯静态托管 |

## 技术说明

**用户认证**：
- 注册：邮箱 + 密码（密码 bcrypt 加盐哈希，最小 8 位）
- 注册后生成激活 token 存入 `email_tokens` 表（有效期 24h），通过 SMTP 发送激活链接
- 用户点击激活链接 → token 校验 → 标记 `emailVerified = true` + 赠送 0.21 积分（同邮箱仅一次）
- 登录成功后下发 HTTP-only Session Cookie（jose 签名 JWT，7 天滑动续期）
- 中间件保护需登录的路由（`/account`、`/history`、`/api/generate`、`/api/upload`、`/api/usage`、`/api/account/*`）
- 未登录访问受保护页面 → 重定向到 `/login`
- 未登录访问 `/generate` → 允许进入和编辑，仅在调用 `/api/generate` 时拦截

**充值码兑换**：
- 充值码存表 `redeem_codes`（保留 v1.0 的 `api_keys` 表数据，作为老 Key 兼容）
- 用户兑换流程：校验码状态（active + 未使用）→ 数据库事务：`users.balance += 充值码积分` + 充值码标记 `used + usedByUserId + usedAt`
- 并发保护：唯一约束 + 事务，防止同一充值码被多次兑换

**图片转存**：
- 生图成功后立即 fetch 中转站 URL → 上传到 OSS 路径 `gallery/users/<uid>/<timestamp>_<rand>.png`
- 历史记录的 `imageUrl` 字段存自有 OSS URL（永久有效）
- 画廊（含模板）、风格预设的封面图统一存自有 OSS URL；批量导入外部封面时先转存到 OSS，再写入数据库
- 拉取失败 / 上传失败：记录错误日志，但不阻断主流程，降级使用中转站原始 URL（标记 `isPersisted = false`，前端展示「图片可能已过期」提示）

**画廊数据结构与加载占位**：
- `gallery_images` 表新增字段：`title`（可选，原模板名）、`category_id`（外键到 categories）、`width` / `height`（图片像素尺寸）
- `prompt_templates` 表已废弃，原数据全部迁入 `gallery_images`：`name → title`、`prompt → prompt`、`cover_image_url → image_url`、`category_id` 映射保留；历史 `model_tag = "GPT-4o Image"` 数据迁移为 `GPT Image2`，前端保留旧标签兼容展示兜底，可在后台手动调整
- 后台上传图片时通过 `image-size` 库 fetch URL 并解析 buffer 取得 `width`/`height` 写库；老数据通过一次性回填接口 `POST /api/admin/gallery/backfill-dimensions` 补齐
- 前台画廊卡片用 `aspect-ratio: width / height` 占位，避免图片陆续加载时 CSS columns 反复重排

**邮件发送**：
- 通过 SMTP 协议（兼容多家服务商，配置在系统设置里）
- 模板：注册激活、密码重置（HTML + 纯文本双版本）
- 失败重试 1 次后写入失败日志

**防刷**：
- 同邮箱注册：DB unique 约束，仅赠送一次（`users.has_received_bonus` 字段）
- 同 IP 24h 内最多 3 次注册：`registration_attempts` 表 + 时间窗口检查
- 充值码兑换：每用户每分钟最多 5 次失败尝试（防止暴力破解）

**图像生成接口（第三方中转站）**：
- 中转站切换方向：后续生图接口从当前已接入的第三方中转站迁移到 VectorEngine 平台，项目仍通过系统设置维护 `baseUrl` 与 API Key，不在代码中硬编码固定域名
- 后续 GPT Image、Google Gemini/Nano Banana、Midjourney 均按 VectorEngine 接口文档适配；Fal.ai Nano Banana 不在接入范围内
- 提交任务：`POST {baseUrl}/v1/images/generations?async=true`
- 当前 OpenAI 生图提交模型 ID 固定为 `gpt-image-2`
- OpenAI 生图前端展示名统一为 `GPT Image2`；历史 `model_tag = "GPT-4o Image"` 数据迁移为 `GPT Image2`，并保留旧标签兼容展示和筛选
- 请求体：`{ model: "gpt-image-2", prompt, size, quality, response_format: "url", image?: string[] }`
- 返回 task_id，异步轮询获取结果
- 轮询：`GET {baseUrl}/v1/images/tasks/{taskId}`
- 成功时返回图片 URL（中转站托管，立即转存到自有 OSS）
- 认证方式：Bearer Token

**VectorEngine 同步接口异步包装预案**：
- 若 VectorEngine 上游接口为同步返回，项目 Web API 不直接等待上游生图完成；Web API 只负责鉴权、余额预检查、参数校验、创建本地任务并立即返回 taskId
- 后台 worker 从数据库领取 `pending` 任务，标记为 `processing` 后按 provider/model 维度限制并发调用同步上游接口
- worker 成功后负责解析图片、转存 OSS、写入历史记录、更新任务为 `completed`；失败则记录 `fail_reason` 和原始响应摘要并更新为 `failed`
- 禁止在 API Route 中使用 fire-and-forget 方式直接启动长耗时生图任务，避免进程重启、部署、超时或异常导致任务丢失
- 初期可使用数据库任务队列 + 独立 worker 进程；量级上升后再评估 Redis/BullMQ 等专用队列
- 建议按通道设置保守并发上限，例如 OpenAI/GPT Image 2-5、Google Gemini Image 1-3、Midjourney 任务提交与轮询独立限流，具体数值以上游限额和服务器资源为准
- 后台系统需配套提供模型管理、任务管理、worker 状态监控、失败重试和模型成本/用户扣费配置，避免只替换接口调用而漏掉运营和排障能力

**尺寸映射规则**：
- 画面比例 + 质量档位 → 映射为具体像素尺寸
- 约束：最大边 ≤ 3840px，总像素 ≤ 8,294,400，比例 ≤ 3:1，16px 倍数

**积分系统**：
- 每张图固定消耗 0.07 积分（可在系统设置中调整）
- 多模型接入后，实际扣费优先读取模型管理中的用户扣费积分；默认单次生图积分仅作为兜底
- 新用户注册赠送 0.21 积分（可在系统设置中调整）
- 生成前检查账号余额，不足则拒绝并提示
- 生成成功后扣减积分（事务保证）；生成失败不扣

**管理后台认证**：
- 固定账号 + 密码，均存储在系统设置中（账号默认 `admin`，可在系统设置中修改）
- 密码 bcrypt 哈希（cost factor 10）
- 登录成功后下发 HTTP-only Session Cookie 维持状态
- 单人使用，无需多用户权限体系
- 错误提示统一为「账号或密码错误」，不区分是账号错还是密码错（防止账号枚举攻击）
- 登录失败锁定：同 IP 连续 5 次错误锁定 15 分钟。`admin_login_attempts` 表记录 IP + 失败时间戳，登录前先校验该 IP 在最近 15 分钟内的失败次数，达到 5 次直接拒绝（不校验账号密码）；登录成功后清除该 IP 的失败记录

## 补充说明

| 项目 | 说明 |
|------|------|
| 画面比例选项 | 2:3 / 1:1 / 16:9 / 9:16 / 4:3 / 3:4 |
| 生图质量选项 | low / medium / high（对应接口 quality 字段） |
| 生成数量选项 | 1 / 2 / 4（多张 = 多次独立调用） |
| 参考图限制 | 最多 12 张，支持 JPG / PNG / WEBP，单张 ≤ 20MB |
| 充值码格式 | CV-XXXXXXXXXXXXXXXX（16 位大写字母+数字+CV- 前缀） |
| 充值码生命周期 | 一次性，兑换后作废 |
| 老 Key 兼容 | v1.0 时代的 Key 与新充值码格式一致，可直接在账号中心兑换 |
| 注册赠送 | 默认 0.21 积分（3 张体验），可在系统设置中调整 |
| 积分精度 | 小数点后两位（0.07 积分/张） |
| 历史记录保留 | 永久保存（图片转存到 OSS），暂不支持用户主动删除 |
| 历史记录左栏条数 | 10 条 |
| 历史记录列表分页 | 每页 20 条，下拉无限滚动 |
| 图片存储 | 用户生成图永久存阿里云 OSS `gallery/users/<uid>/`；画廊（含原模板）图片存 `gallery/`、风格预设封面存 `presets/`，不长期保存外站封面 URL；参考图临时存 `ref-temp/`；原 `templates/` 路径不再写入新数据 |
| 画廊条目字段 | `id` / `title`（可选）/ `prompt` / `image_url` / `width` / `height` / `model_tag` / `style_tag` / `category_id` / `is_featured` / `is_published` / `sort_order` |
| 风格选择交互 | 生图页提示词框内右上角「选择风格」按钮 → 居中弹窗宫格展示风格预设 → 多选 → 应用后按选中顺序拼接 prompt 前缀到提示词 |
| 风格预设描述 | 使用长文本保存完整风格说明，避免批量导入时被短字段截断 |
| 邮件服务商 | 推荐阿里云邮件推送或腾讯云 SES，SMTP 协议 |
| 密码安全 | bcrypt 哈希（cost factor 10），最小 8 位 |
| Session 有效期 | 7 天滑动续期 |
| 模型扩展 | UI 预留多模型位置，当前仅 GPT Image2 可用；Google Nano Banana Pro、Midjourney V7 标注「即将支持」，具体 model id 以中转站文档为准 |
| 后台默认账号 | admin（首次部署使用，可在系统设置中修改） |
| 后台登录锁定 | 同 IP 连续 5 次错误锁定 15 分钟（独立于充值码兑换的 5 次/分钟限制） |
