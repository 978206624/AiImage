# 变更记录

## [v1.4] - 2026-05-14

### 修改

- 修正模型展示口径：Google Imagen 3 → Google Nano Banana Pro，Midjourney v6 → Midjourney V7
- Google/Midjourney 仍为「即将支持」状态（available=false），不影响当前 OpenAI 接口调用逻辑
- Product-Spec 中明确：当前仅 GPT-4o Image 可用，Google Nano Banana Pro 与 Midjourney V7 作为即将支持模型展示，具体 model id 以中转站文档为准
- 前端代码同步更新模型 id、名称、描述、标签、筛选项
- OpenAI 生图接口实际提交给中转站的模型 ID 从 `gpt-image-1` 调整为 `gpt-image-2`

---

## [v1.3] - 2026-05-10

### 新增

- 新增数据字段：`gallery_images.title`（可选，原模板名）、`gallery_images.category_id`（外键到 categories）、`gallery_images.width` / `gallery_images.height`（图片像素尺寸，前台占位用）
- 新增画廊页第三组筛选 chips「分类」（与原有「模型」「风格」并排，自上而下三行）
- 新增生图页风格选择交互：提示词输入框右上角「选择风格」按钮 + 居中模态弹窗，弹窗内宫格展示全部风格预设卡片（封面 + 名称 + 描述截断），可多选，底部「已选 N 个 / 应用 / 清空」操作；应用后选中预设的 prompt 前缀按选中顺序拼接到提示词框正文前
- 新增画廊卡片以真实宽高比占位（基于 `width` / `height`，CSS `aspect-ratio`），加载过程不再发生 columns 重排抖动
- 新增管理员后台接口 `POST /api/admin/gallery/backfill-dimensions`：扫描尺寸为空的画廊图片并回填
- 管理后台「画廊管理」新增「分类」「风格预设」两个 Tab，原模板管理职能并入

### 修改

- 「画廊」吸收原「模板库 / 提示词模板」全部内容：`prompt_templates` 表数据迁入 `gallery_images`，映射 `name → title`、`prompt → prompt`、`cover_image_url → image_url`、`category_id` 保留，迁入记录默认 `model_tag = "GPT-4o Image"` + `style_tag = "通用"`
- 用户使用流程「使用模板创作」与「通过画廊找灵感」合并为单一流程「通过画廊找灵感 / 套用模板」
- 应用场景文案：「从模板库选风格 / 选模板」改为「从画廊按分类挑模板」「在生图页用『选择风格』弹窗叠加风格预设」
- 首页底部 CTA：「引导进入模板库或生图页面」改为「引导进入画廊或生图页面」
- 管理员操作流程第 4 步：原「画廊管理 → 上传图片」+ 第 5 步「模板管理 → 添加模板/预设」合并为「画廊管理：图片 Tab 上传 → 填写 title / prompt / 模型 / 风格 / 分类 → 标记精选 → 上架；分类 Tab 维护分类；风格预设 Tab 维护用于生图页弹窗的预设」
- 公开页面列表：从「首页、画廊、模板库、登录、注册、邮箱激活、忘密重置」改为「首页、画廊、登录、注册、邮箱激活、忘密重置」
- 顶部导航中间链接：从「首页、画廊、模板库、生图」改为「首页、画廊、生图」
- 图片存储说明：原 `templates/` 路径不再写入新数据（原模板封面已迁移到 `gallery/` 路径下）

### 删除

- 移除一级导航「模板库」入口
- 移除独立 `/templates` 页面（含「提示词模板」「风格预设」两个 Tab、分类筛选、应用至生图界面操作栏）
- 移除生图页右栏「风格预设：垂直列表（色块 + 预设名），单选」（功能搬到中栏弹窗，且改为多选）
- 移除生图页中栏「快速套用模板：横向滚动 pill 按钮」（套用模板入口统一到画廊「用此生图」）
- 移除管理后台一级菜单「模板管理」（功能并入画廊管理的三个 Tab）
- 弃用 `prompt_templates` 表的写入路径（数据已迁入 `gallery_images`，旧表保留与否由实施阶段决定）

---

## [v1.2] - 2026-05-10

### 新增

- 后台登录新增「管理员账号」字段：登录页从「仅密码」改为「账号 + 密码」双因素输入
- 新增系统设置项「管理员账号」：默认值 `admin`，可在系统设置中修改
- 新增管理后台登录失败锁定机制：同 IP 连续 5 次失败锁定 15 分钟
- 新增数据表 `admin_login_attempts`：记录失败 IP + 时间戳，登录前校验，登录成功后清除该 IP 失败记录
- 错误提示统一「账号或密码错误」，不区分账号或密码错（防账号枚举）

### 修改

- 风格预设描述字段从短文本提升为长文本，支持保存完整风格说明，避免批量导入时被截断
- 风格预设封面图存储策略调整为先转存到自有 OSS，再写入数据库，不长期依赖外站封面 URL
- 系统设置「管理员密码」拆分为「管理员账号 + 管理员密码」两个字段
- 管理后台登录页 UI：增加账号输入框（位于密码输入框上方）
- 「管理员操作」流程第 1 步从「输入密码登录」改为「输入账号 + 密码登录」
- 管理后台认证技术说明：明确密码 bcrypt 哈希（cost factor 10）、登录态用 HTTP-only Session Cookie

---

## [v1.1] - 2026-05-10

### 新增

- 新增「用户账号系统」：邮箱 + 密码注册/登录、邮箱激活、忘记密码邮件重置、账号中心 `/account`
- 新增「历史记录」功能：生图页左栏简版（最近 10 条，点击复用参数）+ 独立页面 `/history`（4 列网格、模型/比例/时间筛选、分页加载、批量下载）
- 新增「图片转存」机制：生图成功后立即拉取中转站 URL 上传到自有 OSS（路径 `gallery/users/<uid>/`），历史记录永久可见，解决中转站 URL 几小时过期的问题
- 新增「充值码兑换」流程：账号中心 `/account` 内的「充值码兑换」区域，输入码 → 余额到账
- 新增「注册赠送」：新用户邮箱激活后赠送 0.21 积分（3 张免费体验），可在系统设置中调整
- 新增「老 Key 兼容」：v1.0 时代淘宝售出的 Key 可在账号中心作为充值码使用，剩余积分一次性转入账号余额，原 Key 作废
- 新增管理后台「用户管理」模块（管理后台左侧导航首项）：用户列表、手动充值/扣减积分、封禁/解封、重置密码、查看用户的生图与充值历史
- 新增系统设置项：SMTP 邮件配置（Host、Port、用户名、密码、发件人地址）、注册赠送积分配置
- 新增 5 个公开页面：登录 `/login`、注册 `/register`、邮箱激活 `/verify-email`、忘密 `/forgot-password`、重置密码 `/reset-password`
- 新增防刷规则：同邮箱仅赠送一次注册积分、同 IP 24h 内最多注册 3 次、充值码兑换暴力破解防护
- 新增导航栏元素：余额徽章（已登录显示当前积分）+ 头像下拉菜单（账号中心、历史记录、退出登录）
- 新增图片转存降级机制：拉取/上传失败时降级使用中转站原始 URL，标记 `isPersisted = false`，前端展示「图片可能已过期」提示

### 修改

- 商业模式描述：从「淘宝卖 Key（积分卡），用户输入 Key 直接使用」改为「淘宝卖充值码 + 账号制使用，激活账号后兑换充值码积分入账」
- 产品定位：从「无需注册登录」改为「需注册账号使用」，并新增「作品永久保存在账号下」作为核心价值
- 鉴权方式：从「localStorage 存 Key + 每次请求带 Key」改为「HTTP-only Session Cookie + jose 签名 JWT，7 天滑动续期」
- 管理后台「Key 管理」模块改名为「充值码管理」（功能基本一致，保留显示老 Key）
- 生图页左栏：原「历史记录区域（暂无）」占位改为真实「最近创作」列表（10 条简版 + 「查看全部」链接）
- 生图页访问策略：从「需要 Key 才能用」改为「未登录可访问可编辑，点『开始生图』时校验登录」
- 导航栏右侧：从「设置按钮（齿轮）+ CTA」改为「未登录：登录/注册按钮 + CTA；已登录：余额徽章 + 头像下拉 + CTA」
- 用户使用流程：从「输入 Key → 生图」改为「注册 → 激活送积分 → 生图」，新增「老 Key 用户迁移」流程、「找回密码」流程、「复用历史记录创作」流程
- 图片存储策略：从「图片不存储，直接使用中转站原始 URL」改为「生成成功后立即转存自有 OSS，永久保留；失败降级使用中转站 URL」
- 数据存储用途说明扩充：MySQL 新增用户、邮件令牌存储；OSS 新增用户生成图永久存储
- 首页 Hero CTA：根据登录状态动态切换「立即注册」/「开始创作」

### 删除

- 移除导航栏「API Key 设置弹窗」（API Key 输入功能并入账号中心的充值码兑换区域）
- 移除「localStorage 存 Key」前端机制（改 Cookie 鉴权）
- 移除「无需注册登录」的产品宣称
- 移除原「Key 验证与积分」核心功能描述（替换为账号余额体系）

---

## [v1.0] - 初始版本

- Web 平台、Next.js + Tailwind 全栈架构
- 4 个公开页面：首页、画廊、模板库、生图
- 管理后台：Key 管理、画廊管理、模板管理、系统设置
- 商业模式：淘宝卖 Key（积分卡），用户在浏览器 localStorage 存 Key 后直接使用
- AI 能力：通过第三方中转站调用 GPT Image 2 生图
